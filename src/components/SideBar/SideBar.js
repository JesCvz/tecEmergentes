import { Grid } from '@material-ui/core';
import React from 'react';

export const SideBar = () => {
    return(
        <Grid container>
            <Grid item>
                Sidebar
            </Grid>
        </Grid>
    )
}